GAIME_FWUpdate_V4.0.3
work/

# device backup images are large; keep the manifest and identity capture
backup/**/*.img
backup/**/*.bin

# raw camera originals carry GPS EXIF — keep them local
docs/photos/IMG_*.jpeg

# APK build output
apps/*/out/

# app signing keys stay local
apps/*/debug.keystore

# local upstream hardware reference clone; provenance is pinned in conversion-audit.md
hardware/Avaota-A1/
